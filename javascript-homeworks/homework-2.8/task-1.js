/* Постфиксная и префиксная формы
важность: 5
Чему будут равны переменные a, b, c и d в примере ниже? */

let a = 1, b = 1;

let c = ++a; // 2, так как префикс возвращает новое значение
let d = b++; // 1, так как постфикс возвращает старое значение